# Dotfiles

This repository contains my configuration files for Neovim and VSCode.

---

## Neovim

Configuration files are located in `~/.config/nvim`.

To use:

```bash
git clone https://github.com/harsha007009/Dotfiles.git ~/.config/nvim
