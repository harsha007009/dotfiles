return {
  -- Mason for LSP server management
  {
    "williamboman/mason.nvim",
    opts = function(_, opts)
      vim.list_extend(opts.ensure_installed, {
        "lua-language-server",  -- Correct name for Lua LSP
        "clangd",
        "typescript-language-server",  -- Correct name for TypeScript LSP
        "css-lsp",  -- Correct name for CSS LSP
        "html-lsp", -- Correct name for HTML LSP
        "tailwindcss-language-server",
        "luacheck",
        "shellcheck",
        "shfmt",
      })
    end,
  },
}
